/// Defines the order for sorting tasks.
enum TaskSortOrder {
  /// Sort in ascending order (A-Z, oldest to newest, etc.).
  ascending,

  /// Sort in descending order (Z-A, newest to oldest, etc.).
  descending,
}
